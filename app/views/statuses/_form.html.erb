<%= simple_form_for(@status, html: {class: "form-horizontal"}) do |f| %>
  <% if @status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status.errors.count, "error") %> prohibited this status from being saved:</h2>

      <ul>
      <% @status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <% if user_signed_in? %>
      <%= f.input :user_id, collection: User.all, label_method: :full_name, priority: :user %>
      <!-- priority not working -->
    <% else %>
      <%= f.input :user_id, collection: User.all, label_method: :full_name, order: :last_name %>
      <!-- order not working -->
    <% end %>
    <%= f.input :content %>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
